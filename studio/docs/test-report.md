<!-- generated by `npm run test:report` in studio/ — do not edit by hand -->

Generated 2026-09-18

### Summary

| Layer | Result |
|---|---|
| Unit, integration and A/B tests (vitest, node + jsdom) | 542/542 tests pass in 62 files (7 network tests skipped offline) |
| Line coverage of `src/` | 93.0 % lines · 92.8 % functions · 78.6 % branches |
| Browser end-to-end (Playwright over the production build) | 7/7 tests pass |
| Demo scenarios (navigation, vision, control design, group control) | 54/54 pass |

### Coverage per module

| Module | What | Lines | Functions | Branches | Lowest files |
|---|---|---|---|---|---|
| `agri` | fields, orchards, missions | 94.9 % (356/375) | 94.9 % | 82.7 % | `missions.ts` 87 %, `orchard.ts` 99 % |
| `api` | RoboDK-compatible API, RPC, server client | 99.0 % (660/667) | 99.7 % | 85.0 % | `robolink.ts` 99 %, `rpc.ts` 100 % |
| `app / demos (root)` | App class, demo stations | 93.2 % (646/693) | 77.1 % | 78.4 % | `app.ts` 89 %, `demos.ts` 100 % |
| `core` | items, kinematics, motion, collision, calibration | 96.6 % (2371/2454) | 95.8 % | 80.9 % | `kinematics/mechanism.ts` 81 %, `collision/collision.ts` 88 % |
| `ctl` | control design (DES, Petri nets, logic, planning, scheduling, V&V) | 95.0 % (3298/3471) | 92.9 % | 74.8 % | `runtime.ts` 76 %, `realtime.ts` 77 % |
| `fleet` | fleet manager, VDA 5050 | 93.7 % (401/428) | 92.4 % | 75.0 % | `vda5050.ts` 92 %, `fleet.ts` 94 % |
| `io` | station files, URDF, meshes, RoboDK, programs, zip containers | 98.4 % (1559/1585) | 97.0 % | 83.5 % | `robodk/rdk_import.ts` 86 %, `mesh/step.ts` 95 % |
| `mobile` | mobile robots, planners, navigation stack | 96.5 % (603/625) | 99.1 % | 82.2 % | `navstack_ros.ts` 77 %, `controller.ts` 96 % |
| `mrs` | group control (consensus, swarms, allocation, MAPF, coverage, safety, missions) | 97.4 % (1370/1406) | 93.7 % | 83.1 % | `safety.ts` 92 %, `dsl.ts` 94 % |
| `posts` | post processors | 99.9 % (723/724) | 98.3 % | 87.4 % | `python_post.ts` 97 %, `abb.ts` 100 % |
| `ros` | rosbridge publishers | 98.7 % (148/150) | 100.0 % | 85.3 % | `publishers.ts` 97 %, `rosbridge.ts` 100 % |
| `scenarios` | demo scenarios | 98.0 % (342/349) | 98.0 % | 81.6 % | `index.ts` 97 %, `control.ts` 100 % |
| `scene` | three.js renderer (browser only — covered by the e2e suite) | 2.7 % (18/668) | 5.4 % | 1.8 % | `renderer.ts` 0 %, `three-geometry.ts` 1 % |
| `ui` | panels, menus, dialogs, editors (jsdom + e2e) | 97.1 % (2339/2409) | 93.0 % | 81.4 % | `tools.ts` 92 %, `vision_ui.ts` 93 % |
| `vc` | process components | 96.0 % (193/201) | 100.0 % | 84.0 % | `component.ts` 96 % |
| `vision` | machine-vision stack | 96.6 % (1205/1248) | 92.7 % | 76.2 % | `vision_ros.ts` 86 %, `pointcloud.ts` 94 % |

### Test files

| Group | Files | Tests | Time |
|---|---|---|---|
| A/B paired-variant tests | 1 | 16 | 3.2 s |
| UI and App (jsdom) | 4 | 81 | 39.7 s |
| Coverage suites added for the module audit | 9 | 115 | 5.9 s |
| Control design | 9 | 79 | 35.4 s |
| Group control | 7 | 72 | 21.4 s |
| Scenarios | 1 | 56 | 17.8 s |
| Everything else (kinematics, programs, io, vision, fleet, api…) | 31 | 130 | 15.5 s |

### A/B pairs

Each pair runs the same job two ways and asserts the relation between the results.

| Pair | Result |
|---|---|
| A: every demo station serialised · B: the same station reloaded and serialised again — identical documents, identical item trees | ✓ |
| A: a program compiled for the simulator · B: the same program compiled for every post processor — the same motion count reaches every dialect | ✓ |
| A: MoveL between two targets · B: MoveJ between the same targets — same end pose, the linear TCP path stays on the line, the joint path leaves it | ✓ |
| A: the cell as built · B: a box moved into the robot — static check finds no pair in A and a robot–box pair in B, program validation reports it only in B | ✓ |
| A: analytic/numeric IK of every library robot at its home flange pose · B: forward kinematics — B(A(x)) returns to x for every model | ✓ |
| A: grid A* with string-pulling · B: the raw grid path — both collision-free, B never shorter than A, same endpoints | ✓ |
| A: Hungarian assignment · B: greedy, sequential auction, CBBA on the same cost matrix — A is never worse, every method assigns every task once | ✓ |
| A: EDD sequence · B: SPT, FIFO, Moore — SPT minimises total completion, EDD minimises maximum lateness, Moore minimises the number of late jobs | ✓ |
| A: conflict-based search · B: prioritised planning on the same warehouse instance — both conflict-free, A never costs more; ADG execution under delays is collision-free while naive replay collides | ✓ |
| A: connected disk graph · B: the same robots with a shorter radio — λ₂(A) > λ₂(B) = 0 and consensus converges only in A | ✓ |
| A: centralised · B: hybrid · C: decentralised on the mission with a coordinator outage — B and C finish before A, only A stalls, all keep d_safe | ✓ |
| A: the mission model (single integrators, analysis) · B: the same mission on configured station robots driven by pose · C: driven through the unicycle controller — all complete, B ≈ A in time, C travels at least as far as B | ✓ |
| A: the course mission analysed · B: the same document run on a fleet built from it — the fleet reaches the phases the report predicts | ✓ |
| A: the plant alone · B: the plant under a synthesised supervisor — B is a sub-behaviour of A, nonblocking, and never enables the forbidden event | ✓ |
| A: RTK GNSS in open sky · B: the same drive with the sky blocked (GNSS/INS dead reckoning) — A stays within centimetres, B drifts with distance | ✓ |
| A: English · B: Russian — every menu, tab and dialog label used by the UI has a distinct Russian string and the language switch is symmetric | ✓ |

### Browser end-to-end

Playwright drives the production build in Chromium (software GL): a page error or a console error anywhere fails the test.

| Test | Result | Time |
|---|---|---|
| every demo station loads, its program runs to the end without problems and the world clock advances | ✓ | 38 s |
| every menu entry opens without errors; dialogs open and close | ✓ | 166 s |
| bottom tabs render and the RoboDK-API console executes scripts | ✓ | 11 s |
| add a robot from the library, teach targets, run and export with every post processor; undo / redo; save → reload round trip | ✓ | 11 s |
| Control tab: course examples load, every model analyses, the diagram editors open; Group tab: examples analyse and a model runs on the fleet | ✓ | 26 s |
| demo scenarios dialog runs the whole suite headless and every scenario passes | ✓ | 9 s |
| language switch: the menubar and the tabs read in Russian and switch back | ✓ | 5 s |

### Demo scenarios

4 groups (Navigation & localization, Machine vision, Control design (course methods), Group control (multi-robot systems)); the full table with every metric is on the *Demo scenarios* page.
